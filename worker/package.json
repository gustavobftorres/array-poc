{
  "name": "worker",
  "private": true,
  "type": "module",
  "scripts": {
    "dev": "wrangler dev --local --port 8787",
    "db:migrate": "wrangler d1 migrations apply array_poc --local",
    "typecheck": "tsc --noEmit",
    "test": "vitest run"
  },
  "dependencies": { "hono": "^4.6.14", "zod": "^3.24.1" },
  "devDependencies": {
    "@cloudflare/workers-types": "^4.20241230.0",
    "typescript": "^5.7.2",
    "vitest": "^2.1.8",
    "wrangler": "^3.99.0"
  }
}
